APP_NAME=AI Love Strategist
APP_ENV=development
APP_DEBUG=true

HOST=127.0.0.1
PORT=18080

DATABASE_PATH=/opt/ai-love-strategist/data/app.sqlite3
LOG_DIR=/opt/ai-love-strategist/logs
LOG_LEVEL=INFO

LOCAL_USER_ID=00000000-0000-0000-0000-000000000001

# Transitional TEST-114 bootstrap credential. Keep enabled only while migrating to account login sessions.
AUTH_BOOTSTRAP_ENABLED=true
AUTH_BEARER_TOKEN=

# Required when user-managed LLM API keys are persisted.
# Generate with: python -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())"
LLM_CONFIG_ENCRYPTION_KEY=
