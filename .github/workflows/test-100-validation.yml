name: TEST-100 validation

on:
  push:
    branches:
      - test-100-execution-decision-gate

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.11'
      - run: pip install -r backend/requirements.txt
      - run: pytest -q backend/tests/test_execution_decision_gate.py backend/tests/test_execution_scope_isolation.py
      - run: pytest -q
