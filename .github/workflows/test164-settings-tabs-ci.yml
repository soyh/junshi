name: TEST-164 Settings Tabs CI

on:
  push:
    branches:
      - test-164-settings-horizontal-tabs

jobs:
  test:
    runs-on: ubuntu-24.04
    timeout-minutes: 15
    defaults:
      run:
        working-directory: backend
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'
          cache: pip
          cache-dependency-path: backend/requirements.txt

      - name: Install dependencies
        run: pip install -r requirements.txt

      - name: Focused TEST-164 UI tests
        run: pytest -q tests/test_authenticated_lifecycle_v2_workspace.py tests/test_authenticated_guided_workflow_workspace.py tests/test_authenticated_user_presentation_workspace.py

      - name: Authenticated UI regression
        run: pytest -q tests/test_authenticated_*.py

      - name: Full suite
        run: pytest -q
